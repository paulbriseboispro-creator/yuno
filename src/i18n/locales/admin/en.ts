// Section « super admin » (en) — générée depuis src/i18n/locales/admin/modules/*.
// Chargée à la demande par loadLocaleSection('en', 'admin') ; jamais dans le
// dictionnaire principal (un client n'ouvre jamais /admin).
import { pickLanguage } from './modules';

const dict: Record<string, string> = pickLanguage(0);
export default dict;
