// Section « super admin » (fr) — générée depuis src/i18n/locales/admin/modules/*.
// Chargée à la demande par loadLocaleSection('fr', 'admin') ; jamais dans le
// dictionnaire principal (un client n'ouvre jamais /admin).
import { pickLanguage } from './modules';

const dict: Record<string, string> = pickLanguage(1);
export default dict;
