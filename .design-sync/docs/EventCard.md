Carte d'event — l'unité éditoriale centrale du public : affiche pleine, titre, club, date mono, prix « from ».

Prend un objet `event` (`EventCardData`) complet. C'est le composant à réutiliser dès qu'un event est listé ; ne pas le reconstruire à la main.
