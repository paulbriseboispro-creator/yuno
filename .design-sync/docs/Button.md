---
category: actions
---

Bouton d'action publique — CTA d'achat, ajout au panier, suivi d'un club.

Sur les surfaces publiques, privilégier `variant="default"` (rouge #E8192C) pour l'action principale d'un écran, une seule par vue. `size="lg"` pour les CTA de checkout, `icon` pour les actions flottantes (favori, partage).
