---
category: overlays
---

Fenêtre modale centrée — confirmation, détail d'un billet, sélection de table.

Composé : `Dialog` > `DialogTrigger` + `DialogContent` (> `DialogHeader` > `DialogTitle`/`DialogDescription`, puis `DialogFooter`). Sur mobile public, préférer `Drawer`.
