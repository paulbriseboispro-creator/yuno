---
category: overlays
---

Panneau glissant depuis le bas — le format modal par défaut du public mobile.

Composé comme `Dialog` : `Drawer` > `DrawerTrigger` + `DrawerContent` > `DrawerHeader`/`DrawerFooter`. C'est le bon choix pour choisir une quantité de billets ou une bouteille.
